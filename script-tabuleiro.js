var palavras = 

function startGame() {
	palavraSecreta = palavras[Math.floor(Math.random()*palavras.length)];
	console.log(palavraSecreta);
	return palavraSecreta;
}

function startGame() {
    pega a array palavras
    sorteia uma palavra
    exibe forca vazia
    exibe sublinhados da palavra sorteada

}